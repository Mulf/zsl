# Operators and Elementary Operations

## Arithmetic Operations
Arithmetic functions and corresponding overloaded operators in `ZSL` are all element-wise, i.e. the function operate on both input arguments by operating on each element.   The arithmetic operations in `ZSL` are all element-wise. The behavior of `plus/+`,`minus/-` is like that in `Matlab`. But of `times/*`, `rdivide//` have like `.*` 

$

$